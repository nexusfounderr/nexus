import { ScrimItem, TeamRecruitment, FriendItem, ChatMessage, CoachApplication } from '../types';

// Kesinlikle bot veya yapay veri bulunmaz. Sadece gerçek kayıtlı oyuncuların verileri yer alır.
export const DEFAULT_SCRIMS: ScrimItem[] = [];
export const DEFAULT_TEAMS: TeamRecruitment[] = [];
export const DEFAULT_GLOBAL_MESSAGES: ChatMessage[] = [];
export const DEFAULT_COACHES: CoachApplication[] = [];
export const DEFAULT_FRIENDS: FriendItem[] = [];
